diff --git a/imgui.cpp b/imgui.cpp
index a977c773c..3264957f8 100644
--- a/imgui.cpp
+++ b/imgui.cpp
@@ -5437,6 +5437,12 @@ void ImGui::NewFrame()
     g.WindowsActiveCount = 0;
     g.MenusIdSubmittedThisFrame.resize(0);
 
+#ifdef IMGUI_MD3_ENABLED
+    // MD3 UI 系统帧开始钩子
+    extern void MD3_OnNewFrame(float dt);
+    MD3_OnNewFrame(g.IO.DeltaTime);
+#endif
+
     // Calculate frame-rate for the user, as a purely luxurious feature
     g.FramerateSecPerFrameAccum += g.IO.DeltaTime - g.FramerateSecPerFrame[g.FramerateSecPerFrameIdx];
     g.FramerateSecPerFrame[g.FramerateSecPerFrameIdx] = g.IO.DeltaTime;
diff --git a/imgui_widgets.cpp b/imgui_widgets.cpp
index 6be8f7691..11f133bec 100644
--- a/imgui_widgets.cpp
+++ b/imgui_widgets.cpp
@@ -767,6 +767,15 @@ bool ImGui::ButtonBehavior(const ImRect& bb, ImGuiID id, bool* out_hovered, bool
     if (out_hovered) *out_hovered = hovered;
     if (out_held) *out_held = held;
 
+#ifdef IMGUI_MD3_ENABLED
+    // MD3 Ripple 触发钩子
+    if (pressed)
+    {
+        extern void MD3_TriggerRipple(unsigned int, float, float, float, float, float, float);
+        MD3_TriggerRipple(id, g.IO.MousePos.x, g.IO.MousePos.y, bb.Min.x, bb.Min.y, bb.Max.x, bb.Max.y);
+    }
+#endif
+
     return pressed;
 }
 
@@ -813,6 +822,11 @@ bool ImGui::ButtonEx(const char* label, const ImVec2& size_arg, ImGuiButtonFlags
 
 bool ImGui::Button(const char* label, const ImVec2& size_arg)
 {
+#ifdef IMGUI_MD3_ENABLED
+    // MD3 Button 替换钩子
+    extern bool MD3_Button(const char*, float, float);
+    return MD3_Button(label, size_arg.x, size_arg.y);
+#endif
     return ButtonEx(label, size_arg, ImGuiButtonFlags_None);
 }
 
@@ -1219,6 +1233,12 @@ bool ImGui::ImageButton(ImTextureID user_texture_id, const ImVec2& size, const I
 
 bool ImGui::Checkbox(const char* label, bool* v)
 {
+#ifdef IMGUI_MD3_ENABLED
+    // MD3 Checkbox 替换钩子
+    extern bool MD3_Checkbox(const char*, bool*);
+    return MD3_Checkbox(label, v);
+#endif
+
     ImGuiWindow* window = GetCurrentWindow();
     if (window->SkipItems)
         return false;
@@ -8790,13 +8810,16 @@ int ImGui::PlotEx(ImGuiPlotType plot_type, const char* label, float (*values_get
             const int v_idx = (int)(t * item_count);
             IM_ASSERT(v_idx >= 0 && v_idx < values_count);
 
+            idx_hovered = v_idx;
+
+#ifndef IMGUI_DISABLE_PLOT_TOOLTIPS
             const float v0 = values_getter(data, (v_idx + values_offset) % values_count);
             const float v1 = values_getter(data, (v_idx + 1 + values_offset) % values_count);
             if (plot_type == ImGuiPlotType_Lines)
                 SetTooltip("%d: %8.4g\n%d: %8.4g", v_idx, v0, v_idx + 1, v1);
             else if (plot_type == ImGuiPlotType_Histogram)
                 SetTooltip("%d: %8.4g", v_idx, v0);
-            idx_hovered = v_idx;
+#endif
         }
 
         const float t_step = 1.0f / (float)res_w;
diff --git a/imconfig.h b/imconfig.h
index c6d9b8d5f..6c0cee19d 100644
--- a/imconfig.h
+++ b/imconfig.h
@@ -37,6 +37,11 @@
 //#define IMGUI_DISABLE_DEMO_WINDOWS                        // Disable demo windows: ShowDemoWindow()/ShowStyleEditor() will be empty.
 //#define IMGUI_DISABLE_DEBUG_TOOLS                         // Disable metrics/debugger and other debug tools: ShowMetricsWindow(), ShowDebugLogWindow() and ShowIDStackToolWindow() will be empty.
 
+//---- ParticleSaturn custom options
+// Disable ImGui::PlotLines()/PlotHistogram() built-in hover tooltip (SetTooltip in PlotEx).
+// This keeps your own custom tooltip while preventing the default "idx: value" lines.
+#define IMGUI_DISABLE_PLOT_TOOLTIPS
+
 //---- Don't implement some functions to reduce linkage requirements.
 //#define IMGUI_DISABLE_WIN32_DEFAULT_CLIPBOARD_FUNCTIONS   // [Win32] Don't implement default clipboard handler. Won't use and link with OpenClipboard/GetClipboardData/CloseClipboard etc. (user32.lib/.a, kernel32.lib/.a)
 //#define IMGUI_ENABLE_WIN32_DEFAULT_IME_FUNCTIONS          // [Win32] [Default with Visual Studio] Implement default IME handler (require imm32.lib/.a, auto-link for Visual Studio, -limm32 on command-line for MinGW)
