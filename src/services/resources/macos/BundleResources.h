#pragma once

#include <string>

namespace ParticleSaturn::Services::Resources::MacOS {

std::string LocateModel(const std::string& filename);

} // namespace ParticleSaturn::Services::Resources::MacOS
